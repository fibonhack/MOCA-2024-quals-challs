name: "RSA w/ leak"
author: gio-d3141

description: Just a standard RSA challenge with a leak
# port: 10004
# host: "challs.{{ CTFD_URL }}"
# connection_info: "nc {{ host }} {{ port }}"
# healthcheck: "checker.py HOST={{ host }} PORT={{ port }}"

topics:
  - crypto

files:
  - attachments/chall.py
  - attachments/output.txt
