name: "Compressor as a service"
author: drw0if

description: I made this compressor service to help me in my web stuff
port: 10004
host: "challs.{{ CTFD_URL }}"
connection_info: "nc {{ host }} {{ port }}"
# healthcheck: "checker.py HOST={{ host }} PORT={{ port }}"

topics:
- pwn

files:
- attachments/chall
