<!DOCTYPE html>
<html>

<head>
  <title> Device Setup </title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#4bd397',
            secondary: '#3a736c',
          },
        },
      },
    };
  </script>
</head>

<body class="bg-slate-900 mx-10">
  <div class="flex flex-col items-center justify-center h-dvh w-full text-slate-200">
    <img src="/images/weather-station-2.png" alt="Background" class="absolute m-auto z-0 object-cover h-2/3 object-center">
    <form action="" method="POST" class="backdrop-blur-sm bg-slate-500/20 shadow-md rounded px-8 pt-4 pb-6 mb-4 w-3/4 z-10">
      <h1 class="text-2xl font-semibold mb-2 text-slate-100 text-center">Device configuration</h1>
      <% if (error) { %>
        <div class="text-red-700 text-center text-lg">
          <%= error %>
        </div>
        <% } %>
          <div class="mb-2">
            <label class="block text-light text-sm font-bold mb-2" for="cpu_temperature">Cpu temperature</label>
            <input type="text" id="cpu_temperature" name="cpu_temperature" required
              class="shadow appearance-none rounded w-full py-2 px-3 backdrop-blur-sm bg-slate-700/20 mb-3 leading-tight outline-none focus:shadow-outline">
          </div>

          <div class="mb-4">
            <label class="block text-light text-sm font-bold mb-2" for="temperature">Temperature</label>
            <input type="text" id="temperature" name="temperature" required
              class="shadow appearance-none rounded w-full py-2 px-3 backdrop-blur-sm bg-slate-700/20 mb-3 leading-tight outline-none focus:shadow-outline">
          </div>

          <div class="mb-4">
            <label class="block text-light text-sm font-bold mb-2" for="humidity">Humidity</label>
            <input type="text" id="humidity" name="humidity" required
              class="shadow appearance-none rounded w-full py-2 px-3 backdrop-blur-sm bg-slate-700/20 mb-3 leading-tight outline-none focus:shadow-outline">
          </div>

          <div class="flex items-center justify-between">
            <button
              class="bg-secondary hover:bg-primary focus:bg-primary text-light font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
              type="submit">Submit</button>
            <a class="inline-block align-baseline font-bold text-sm text-secondary hover:text-primary" href="/">Go
              Back</a>
          </div>
    </form>
  </div>
</body>

</html>