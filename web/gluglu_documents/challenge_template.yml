name: "Gluglu documents"
author: drw0if, b0n0b0

description: Real world shenanigans, would you ever have said that? (run `/readflag` to get the flag)
port: 20005
host: "challs.{{ CTFD_URL }}"
connection_info: "http://{{ host }}:{{ port }}"
# healthcheck: "checker.py HOST={{ host }} PORT={{ port }}"

topics:
- web

files: